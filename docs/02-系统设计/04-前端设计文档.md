# 前端设计文档

## 一、技术选型

### 1.1 核心依赖

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue.js | 3.4+ | 前端框架 |
| Vite | 5.0+ | 构建工具 |
| Element Plus | 2.5+ | UI 组件库 |
| Pinia | 2.1+ | 状态管理 |
| Vue Router | 4.2+ | 路由管理 |
| Axios | 1.6+ | HTTP 客户端 |
| ECharts | 5.5+ | 图表库 |
| dayjs | 1.11+ | 日期处理 |
| lodash-es | 4.17+ | 工具库 |

### 1.2 项目结构

```
frontend/
├── public/                   # 静态资源
│   ├── favicon.ico
│   └── robots.txt
├── src/
│   ├── api/                  # API 接口封装
│   │   ├── index.js         # API 入口
│   │   ├── auth.js          # 认证相关
│   │   ├── scenarios.js     # 场景相关
│   │   ├── reports.js       # 报告相关
│   │   ├── environments.js  # 环境变量相关
│   │   ├── datasources.js   # 数据源相关
│   │   └── testsuites.js    # 测试套件相关
│   ├── assets/               # 资源文件
│   │   ├── images/
│   │   └── styles/          # 全局样式
│   ├── components/           # 公共组件
│   │   ├── Layout/          # 布局组件
│   │   ├── Charts/          # 图表组件
│   │   ├── Form/            # 表单组件
│   │   ├── Table/           # 表格组件
│   │   └── Common/          # 通用组件
│   ├── pages/               # 页面组件
│   │   ├── Dashboard.vue
│   │   ├── Login.vue
│   │   ├── Scenarios.vue
│   │   ├── ScenarioEditor.vue
│   │   ├── ScenarioImport.vue
│   │   ├── RunConfig.vue
│   │   ├── RunMonitor.vue
│   │   ├── Reports.vue
│   │   ├── ReportDetail.vue
│   │   ├── ReportCompare.vue
│   │   ├── Environments.vue
│   │   ├── DataSources.vue
│   │   ├── TestSuites.vue
│   │   └── TestSuiteEditor.vue
│   ├── router/              # 路由配置
│   ├── store/               # 状态管理
│   ├── utils/               # 工具函数
│   ├── App.vue
│   └── main.js
├── package.json
└── vite.config.js
```

---

## 二、路由设计

### 2.1 路由配置

```javascript
// router/index.js
const routes = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/pages/Login.vue'),
    meta: { requiresAuth: false }
  },
  {
    path: '/register',
    name: 'Register',
    component: () => import('@/pages/Register.vue'),
    meta: { requiresAuth: false }
  },
  {
    path: '/',
    component: () => import('@/components/Layout/index.vue'),
    meta: { requiresAuth: true },
    children: [
      {
        path: '',
        name: 'Dashboard',
        component: () => import('@/pages/Dashboard.vue')
      },
      {
        path: 'scenarios',
        name: 'Scenarios',
        component: () => import('@/pages/Scenarios.vue')
      },
      {
        path: 'scenarios/create',
        name: 'ScenarioCreate',
        component: () => import('@/pages/ScenarioEditor.vue')
      },
      {
        path: 'scenarios/:id/edit',
        name: 'ScenarioEdit',
        component: () => import('@/pages/ScenarioEditor.vue')
      },
      {
        path: 'scenarios/import',
        name: 'ScenarioImport',
        component: () => import('@/pages/ScenarioImport.vue')
      },
      {
        path: 'runs/:id/config',
        name: 'RunConfig',
        component: () => import('@/pages/RunConfig.vue')
      },
      {
        path: 'runs/:id/monitor',
        name: 'RunMonitor',
        component: () => import('@/pages/RunMonitor.vue')
      },
      {
        path: 'reports',
        name: 'Reports',
        component: () => import('@/pages/Reports.vue')
      },
      {
        path: 'reports/:id',
        name: 'ReportDetail',
        component: () => import('@/pages/ReportDetail.vue')
      },
      {
        path: 'reports/compare',
        name: 'ReportCompare',
        component: () => import('@/pages/ReportCompare.vue')
      },
      {
        path: 'environments',
        name: 'Environments',
        component: () => import('@/pages/Environments.vue')
      },
      {
        path: 'datasources',
        name: 'DataSources',
        component: () => import('@/pages/DataSources.vue')
      },
      {
        path: 'testsuites',
        name: 'TestSuites',
        component: () => import('@/pages/TestSuites.vue')
      },
      {
        path: 'testsuites/:id/edit',
        name: 'TestSuiteEdit',
        component: () => import('@/pages/TestSuiteEditor.vue')
      }
    ]
  },
  {
    path: '/:pathMatch(.*)*',
    name: 'NotFound',
    component: () => import('@/pages/NotFound.vue')
  }
]
```

### 2.2 路由守卫

```javascript
// router/guard.js
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()
  
  if (to.meta.requiresAuth && !userStore.isLoggedIn) {
    next({ name: 'Login', query: { redirect: to.fullPath } })
  } else if (to.path === '/login' && userStore.isLoggedIn) {
    next({ name: 'Dashboard' })
  } else {
    next()
  }
})
```

---

## 三、状态管理

### 3.1 Store 结构

```
store/
├── index.js              # Pinia 实例
├── modules/
│   ├── user.js          # 用户状态
│   ├── scenarios.js     # 场景状态
│   ├── reports.js       # 报告状态
│   ├── environments.js  # 环境变量状态
│   └── datasources.js   # 数据源状态
└── plugins/
    └── persist.js       # 持久化插件
```

### 3.2 用户状态

```javascript
// store/modules/user.js
import { defineStore } from 'pinia'
import { login, logout, getUserInfo, refreshToken } from '@/api/auth'

export const useUserStore = defineStore('user', {
  state: () => ({
    token: null,
    refreshToken: null,
    userInfo: null,
    isLoggedIn: false
  }),
  
  getters: {
    username: (state) => state.userInfo?.username || '',
    isAdmin: (state) => state.userInfo?.role === 'admin'
  },
  
  actions: {
    async doLogin(credentials) {
      try {
        const response = await login(credentials)
        this.token = response.data.token.access_token
        this.refreshToken = response.data.token.refresh_token
        this.userInfo = response.data.user
        this.isLoggedIn = true
        return response
      } catch (error) {
        throw error
      }
    },
    
    async doLogout() {
      try {
        await logout()
      } finally {
        this.clearUser()
      }
    },
    
    clearUser() {
      this.token = null
      this.refreshToken = null
      this.userInfo = null
      this.isLoggedIn = false
    }
  },
  
  persist: {
    paths: ['token', 'refreshToken', 'userInfo', 'isLoggedIn']
  }
})
```

### 3.3 场景状态

```javascript
// store/modules/scenarios.js
import { defineStore } from 'pinia'
import { getScenarios, getScenario, createScenario, updateScenario, deleteScenario } from '@/api/scenarios'

export const useScenarioStore = defineStore('scenarios', {
  state: () => ({
    currentScenario: null,
    scenariosList: [],
    pagination: {
      total: 0,
      page: 1,
      pageSize: 20
    },
    loading: false
  }),
  
  actions: {
    async fetchScenarios(params = {}) {
      this.loading = true
      try {
        const response = await getScenarios(params)
        this.scenariosList = response.data.items
        this.pagination = response.data.meta.pagination
      } finally {
        this.loading = false
      }
    },
    
    async fetchScenario(id) {
      this.loading = true
      try {
        const response = await getScenario(id)
        this.currentScenario = response.data
        return response.data
      } finally {
        this.loading = false
      }
    },
    
    async createScenario(data) {
      const response = await createScenario(data)
      this.scenariosList.unshift(response.data)
      return response.data
    },
    
    async updateScenario(id, data) {
      const response = await updateScenario(id, data)
      const index = this.scenariosList.findIndex(s => s.id === id)
      if (index !== -1) {
        this.scenariosList[index] = response.data
      }
      return response.data
    },
    
    async deleteScenario(id) {
      await deleteScenario(id)
      this.scenariosList = this.scenariosList.filter(s => s.id !== id)
    }
  }
})
```

---

## 四、组件设计

### 4.1 布局组件

```vue
<!-- components/Layout/index.vue -->
<template>
  <el-container class="layout-container">
    <el-aside width="240px" class="sidebar">
      <div class="logo">
        <img src="@/assets/images/logo.png" alt="Logo" />
        <span>压测平台</span>
      </div>
      <el-menu
        :default-active="activeMenu"
        router
        background-color="#304156"
        text-color="#bfcbd9"
        active-text-color="#409EFF"
      >
        <el-menu-item index="/">
          <el-icon><HomeFilled /></el-icon>
          <span>首页</span>
        </el-menu-item>
        <el-menu-item index="/scenarios">
          <el-icon><Document /></el-icon>
          <span>场景管理</span>
        </el-menu-item>
        <el-menu-item index="/reports">
          <el-icon><DataAnalysis /></el-icon>
          <span>报告管理</span>
        </el-menu-item>
        <el-menu-item index="/environments">
          <el-icon><Setting /></el-icon>
          <span>环境变量</span>
        </el-menu-item>
        <el-menu-item index="/datasources">
          <el-icon><FolderOpened /></el-icon>
          <span>数据源管理</span>
        </el-menu-item>
        <el-menu-item index="/testsuites">
          <el-icon><CheckCircle /></el-icon>
          <span>接口测试</span>
        </el-menu-item>
      </el-menu>
    </el-aside>
    
    <el-container>
      <el-header class="header">
        <div class="header-left">
          <Breadcrumb />
        </div>
        <div class="header-right">
          <el-dropdown @command="handleCommand">
            <span class="user-info">
              {{ userStore.username }}
              <el-icon><ArrowDown /></el-icon>
            </span>
            <template #dropdown>
              <el-dropdown-menu>
                <el-dropdown-item command="profile">个人中心</el-dropdown-item>
                <el-dropdown-item command="logout" divided>退出登录</el-dropdown-item>
              </el-dropdown-menu>
            </template>
          </el-dropdown>
        </div>
      </el-header>
      
      <el-main class="main">
        <router-view v-slot="{ Component }">
          <transition name="fade" mode="out-in">
            <keep-alive>
              <component :is="Component" />
            </keep-alive>
          </transition>
        </router-view>
      </el-main>
    </el-container>
  </el-container>
</template>

<script setup>
import { computed } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import { useUserStore } from '@/store/modules/user'
import { ElMessageBox } from 'element-plus'

const route = useRoute()
const router = useRouter()
const userStore = useUserStore()

const activeMenu = computed(() => route.path)

const handleCommand = async (command) => {
  switch (command) {
    case 'profile':
      router.push('/profile')
      break
    case 'logout':
      await ElMessageBox.confirm('确定要退出登录吗？', '提示')
      await userStore.doLogout()
      router.push('/login')
      break
  }
}
</script>

<style lang="scss" scoped>
.layout-container {
  height: 100vh;
  
  .sidebar {
    background-color: #304156;
    
    .logo {
      height: 60px;
      display: flex;
      align-items: center;
      padding: 0 20px;
      color: #fff;
      font-size: 18px;
      font-weight: bold;
      
      img {
        width: 32px;
        height: 32px;
        margin-right: 12px;
      }
    }
  }
  
  .header {
    background: #fff;
    box-shadow: 0 1px 4px rgba(0, 21, 41, 0.08);
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 24px;
  }
  
  .main {
    background-color: #f0f2f5;
    padding: 24px;
  }
}
</style>
```

### 4.2 图表组件

```vue
<!-- components/Charts/LineChart.vue -->
<template>
  <div ref="chartRef" class="line-chart"></div>
</template>

<script setup>
import { ref, onMounted, onUnmounted, watch } from 'vue'
import * as echarts from 'echarts'
import { useResizeObserver } from '@vueuse/core'

const props = defineProps({
  data: {
    type: Array,
    default: () => []
  },
  title: {
    type: String,
    default: ''
  },
  xKey: {
    type: String,
    default: 'timestamp'
  },
  yKey: {
    type: String,
    default: 'value'
  },
  color: {
    type: String,
    default: '#409EFF'
  },
  areaStyle: {
    type: Boolean,
    default: false
  }
})

const chartRef = ref(null)
let chartInstance = null

const initChart = () => {
  if (!chartRef.value) return
  
  chartInstance = echarts.init(chartRef.value)
  updateChart()
}

const updateChart = () => {
  if (!chartInstance) return
  
  const option = {
    title: {
      text: props.title,
      left: 'center',
      textStyle: {
        fontSize: 14,
        fontWeight: 'normal'
      }
    },
    tooltip: {
      trigger: 'axis',
      formatter: (params) => {
        const data = params[0]
        return `${data.axisLabel}: ${data.value}`
      }
    },
    grid: {
      left: '3%',
      right: '4%',
      bottom: '3%',
      containLabel: true
    },
    xAxis: {
      type: 'category',
      boundaryGap: false,
      data: props.data.map(item => item[props.xKey])
    },
    yAxis: {
      type: 'value',
      axisLabel: {
        formatter: '{value}'
      }
    },
    series: [
      {
        name: props.yKey,
        type: 'line',
        smooth: true,
        symbol: 'none',
        areaStyle: props.areaStyle ? {} : undefined,
        lineStyle: {
          color: props.color,
          width: 2
        },
        itemStyle: {
          color: props.color
        },
        data: props.data.map(item => item[props.yKey])
      }
    ]
  }
  
  chartInstance.setOption(option)
}

watch(() => props.data, updateChart, { deep: true })

onMounted(() => {
  initChart()
  useResizeObserver(chartRef, () => {
    chartInstance?.resize()
  })
})

onUnmounted(() => {
  chartInstance?.dispose()
})
</script>

<style lang="scss" scoped>
.line-chart {
  width: 100%;
  height: 300px;
}
</style>
```

### 4.3 请求编辑器组件

```vue
<!-- components/Form/RequestEditor.vue -->
<template>
  <el-form :model="form" :rules="rules" ref="formRef" label-width="100px">
    <el-form-item label="请求名称" prop="name">
      <el-input v-model="form.name" placeholder="请输入请求名称" />
    </el-form-item>
    
    <el-form-item label="请求方法" prop="method">
      <el-select v-model="form.method" style="width: 120px">
        <el-option label="GET" value="GET" />
        <el-option label="POST" value="POST" />
        <el-option label="PUT" value="PUT" />
        <el-option label="DELETE" value="DELETE" />
        <el-option label="PATCH" value="PATCH" />
      </el-select>
    </el-form-item>
    
    <el-form-item label="请求URL" prop="url">
      <el-input v-model="form.url" placeholder="请输入请求URL，支持变量替换，如: /api/users/${user_id}" />
    </el-form-item>
    
    <el-form-item label="请求头">
      <el-row v-for="(header, index) in form.headers" :key="index" :gutter="10" class="header-row">
        <el-col :span="10">
          <el-input v-model="header.name" placeholder="Header 名称" />
        </el-col>
        <el-col :span="10">
          <el-input v-model="header.value" placeholder="Header 值，支持变量替换" />
        </el-col>
        <el-col :span="4">
          <el-button type="danger" :icon="Delete" circle @click="removeHeader(index)" />
        </el-col>
      </el-row>
      <el-button type="primary" link @click="addHeader">添加 Header</el-button>
    </el-form-item>
    
    <el-form-item label="请求体" v-if="['POST', 'PUT', 'PATCH'].includes(form.method)">
      <el-radio-group v-model="form.bodyType">
        <el-radio label="none">无</el-radio>
        <el-radio label="json">JSON</el-radio>
        <el-radio label="form">表单</el-radio>
      </el-radio-group>
      
      <el-input
        v-if="form.bodyType === 'json'"
        v-model="form.body"
        type="textarea"
        :rows="5"
        placeholder='请输入 JSON 格式，支持变量替换，如: {"username": "${username}"}'
      />
      
      <div v-else-if="form.bodyType === 'form'">
        <el-row v-for="(field, index) in form.bodyFields" :key="index" :gutter="10" class="body-field-row">
          <el-col :span="10">
            <el-input v-model="field.name" placeholder="字段名" />
          </el-col>
          <el-col :span="10">
            <el-input v-model="field.value" placeholder="字段值，支持变量替换" />
          </el-col>
          <el-col :span="4">
            <el-button type="danger" :icon="Delete" circle @click="removeBodyField(index)" />
          </el-col>
        </el-row>
        <el-button type="primary" link @click="addBodyField">添加字段</el-button>
      </div>
    </el-form-item>
    
    <el-form-item label="高级配置">
      <el-collapse>
        <el-collapse-item title="高级配置" name="advanced">
          <el-row :gutter="20">
            <el-col :span="8">
              <el-form-item label="请求权重">
                <el-input-number v-model="form.weight" :min="1" :max="100" />
              </el-form-item>
            </el-col>
            <el-col :span="8">
              <el-form-item label="思考时间(秒)">
                <el-input-number v-model="form.thinkTime" :min="0" :precision="1" />
              </el-form-item>
            </el-col>
            <el-col :span="8">
              <el-form-item label="超时时间(秒)">
                <el-input-number v-model="form.timeout" :min="1" :max="300" />
              </el-form-item>
            </el-col>
          </el-row>
          
          <el-form-item label="数据源绑定">
            <el-select v-model="form.dataSourceId" placeholder="选择数据源" clearable>
              <el-option
                v-for="ds in dataSources"
                :key="ds.id"
                :label="ds.name"
                :value="ds.id"
              />
            </el-select>
          </el-form-item>
        </el-collapse-item>
      </el-collapse>
    </el-form-item>
    
    <el-form-item>
      <el-button type="primary" @click="handleSubmit">保存</el-button>
      <el-button @click="handleTest">测试请求</el-button>
    </el-form-item>
  </el-form>
</template>

<script setup>
import { ref, reactive, onMounted } from 'vue'
import { Delete } from '@element-plus/icons-vue'
import { ElMessage } from 'element-plus'
import { testRequest } from '@/api/requests'

const props = defineProps({
  request: {
    type: Object,
    default: () => ({})
  },
  dataSources: {
    type: Array,
    default: () => []
  }
})

const emit = defineEmits(['submit', 'test'])

const formRef = ref(null)
const form = reactive({
  name: '',
  method: 'GET',
  url: '',
  headers: [],
  bodyType: 'none',
  body: '',
  bodyFields: [],
  weight: 1,
  thinkTime: 0,
  timeout: 30,
  dataSourceId: null
})

const rules = {
  name: [{ required: true, message: '请输入请求名称', trigger: 'blur' }],
  method: [{ required: true, message: '请选择请求方法', trigger: 'change' }],
  url: [{ required: true, message: '请输入请求URL', trigger: 'blur' }]
}

onMounted(() => {
  if (props.request.id) {
    Object.assign(form, props.request)
    form.headers = props.request.headers || []
    form.bodyFields = props.request.bodyFields || []
  }
})

const addHeader = () => {
  form.headers.push({ name: '', value: '' })
}

const removeHeader = (index) => {
  form.headers.splice(index, 1)
}

const addBodyField = () => {
  form.bodyFields.push({ name: '', value: '' })
}

const removeBodyField = (index) => {
  form.bodyFields.splice(index, 1)
}

const handleSubmit = async () => {
  await formRef.value.validate()
  emit('submit', { ...form })
}

const handleTest = async () => {
  try {
    const response = await testRequest({ ...form })
    ElMessage.success(`请求成功，响应时间: ${response.data.response_time}ms`)
    emit('test', response.data)
  } catch (error) {
    ElMessage.error('请求失败')
  }
}
</script>

<style lang="scss" scoped>
.header-row,
.body-field-row {
  margin-bottom: 8px;
}
</style>
```

---

## 五、页面设计

### 5.1 场景列表页面

```vue
<!-- pages/Scenarios.vue -->
<template>
  <div class="scenarios-page">
    <div class="page-header">
      <h2>场景管理</h2>
      <div class="actions">
        <el-button type="primary" @click="handleCreate">
          <el-icon><Plus /></el-icon>
          新建场景
        </el-button>
        <el-button @click="handleImport">
          <el-icon><Upload /></el-icon>
          导入HAR
        </el-button>
      </div>
    </div>
    
    <el-card class="filter-card">
      <el-form :inline="true" :model="filters">
        <el-form-item label="搜索">
          <el-input v-model="filters.search" placeholder="搜索场景名称" clearable @input="handleSearch" />
        </el-form-item>
        <el-form-item>
          <el-button type="primary" @click="fetchScenarios">查询</el-button>
          <el-button @click="resetFilters">重置</el-button>
        </el-form-item>
      </el-form>
    </el-card>
    
    <el-card class="table-card">
      <el-table
        :data="scenarioStore.scenariosList"
        v-loading="scenarioStore.loading"
        stripe
        style="width: 100%"
      >
        <el-table-column prop="name" label="场景名称" min-width="200" />
        <el-table-column prop="target_host" label="目标主机" min-width="200" />
        <el-table-column prop="request_count" label="请求数" width="100" align="center" />
        <el-table-column prop="created_at" label="创建时间" width="180">
          <template #default="{ row }">
            {{ formatDate(row.created_at) }}
          </template>
        </el-table-column>
        <el-table-column label="操作" width="200" fixed="right">
          <template #default="{ row }">
            <el-button type="primary" link @click="handleEdit(row)">编辑</el-button>
            <el-button type="success" link @click="handleRun(row)">执行</el-button>
            <el-button type="warning" link @click="handleDuplicate(row)">复制</el-button>
            <el-button type="danger" link @click="handleDelete(row)">删除</el-button>
          </template>
        </el-table-column>
      </el-table>
      
      <div class="pagination">
        <el-pagination
          v-model:current-page="scenarioStore.pagination.page"
          v-model:page-size="scenarioStore.pagination.pageSize"
          :total="scenarioStore.pagination.total"
          :page-sizes="[10, 20, 50, 100]"
          layout="total, sizes, prev, pager, next, jumper"
          @size-change="handleSizeChange"
          @current-change="handlePageChange"
        />
      </div>
    </el-card>
  </div>
</template>

<script setup>
import { reactive, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { ElMessage, ElMessageBox } from 'element-plus'
import { useScenarioStore } from '@/store/modules/scenarios'
import { formatDate } from '@/utils/format'

const router = useRouter()
const scenarioStore = useScenarioStore()

const filters = reactive({
  search: ''
})

onMounted(() => {
  fetchScenarios()
})

const fetchScenarios = () => {
  scenarioStore.fetchScenarios(filters)
}

const handleSearch = debounce(() => {
  fetchScenarios()
}, 300)

const resetFilters = () => {
  filters.search = ''
  fetchScenarios()
}

const handleCreate = () => {
  router.push('/scenarios/create')
}

const handleImport = () => {
  router.push('/scenarios/import')
}

const handleEdit = (row) => {
  router.push(`/scenarios/${row.id}/edit`)
}

const handleRun = (row) => {
  router.push(`/runs/${row.id}/config`)
}

const handleDuplicate = async (row) => {
  await scenarioStore.duplicateScenario(row.id)
  ElMessage.success('复制成功')
  fetchScenarios()
}

const handleDelete = async (row) => {
  await ElMessageBox.confirm('确定要删除该场景吗？', '警告', {
    type: 'warning'
  })
  await scenarioStore.deleteScenario(row.id)
  ElMessage.success('删除成功')
}

const handleSizeChange = (size) => {
  scenarioStore.pagination.pageSize = size
  fetchScenarios()
}

const handlePageChange = (page) => {
  scenarioStore.pagination.page = page
  fetchScenarios()
}
</script>

<style lang="scss" scoped>
.scenarios-page {
  .page-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 24px;
    
    h2 {
      margin: 0;
      font-size: 24px;
      font-weight: 600;
    }
  }
  
  .filter-card {
    margin-bottom: 24px;
  }
  
  .pagination {
    display: flex;
    justify-content: flex-end;
    margin-top: 20px;
  }
}
</style>
```

### 5.2 实时监控页面

```vue
<!-- pages/RunMonitor.vue -->
<template>
  <div class="run-monitor">
    <div class="page-header">
      <h2>压测执行: {{ scenario?.name }}</h2>
      <div class="actions">
        <el-tag :type="statusTagType" size="large">{{ statusText }}</el-tag>
        <el-button v-if="runStatus === 'running'" type="danger" @click="handleStop">
          停止压测
        </el-button>
      </div>
    </div>
    
    <el-row :gutter="20" class="stats-row">
      <el-col :span="6">
        <el-card class="stat-card">
          <template #header>
            <div class="stat-header">
              <el-icon><User /></el-icon>
              <span>当前用户</span>
            </div>
          </template>
          <div class="stat-value">{{ stats.user_count || 0 }}</div>
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card class="stat-card">
          <template #header>
            <div class="stat-header">
              <el-icon><Connection /></el-icon>
              <span>总请求数</span>
            </div>
          </template>
          <div class="stat-value">{{ formatNumber(stats.total_requests) }}</div>
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card class="stat-card">
          <template #header>
            <div class="stat-header">
              <el-icon><Lightning /></el-icon>
              <span>RPS</span>
            </div>
          </template>
          <div class="stat-value">{{ (stats.requests_per_second || 0).toFixed(2) }}</div>
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card class="stat-card">
          <template #header>
            <div class="stat-header">
              <el-icon><WarningFilled /></el-icon>
              <span>错误率</span>
            </div>
          </template>
          <div class="stat-value">{{ ((stats.error_rate || 0) * 100).toFixed(2) }}%</div>
        </el-card>
      </el-col>
    </el-row>
    
    <el-row :gutter="20" class="charts-row">
      <el-col :span="16">
        <el-card>
          <template #header>
            <span>RPS 趋势</span>
          </template>
          <LineChart
            :data="chartData.rps"
            :x-key="'timestamp'"
            :y-key="'value'"
            color="#67C23A"
          />
        </el-card>
      </el-col>
      <el-col :span="8">
        <el-card>
          <template #header>
            <span>错误分布</span>
          </template>
          <PieChart :data="chartData.errors" />
        </el-card>
      </el-col>
    </el-row>
    
    <el-row :gutter="20" class="charts-row">
      <el-col :span="24">
        <el-card>
          <template #header>
            <span>响应时间趋势</span>
          </template>
          <LineChart
            :data="chartData.responseTime"
            :x-key="'timestamp'"
            :y-key="'p50'"
            :area-style="true"
          />
        </el-card>
      </el-col>
    </el-row>
    
    <el-card class="requests-card">
      <template #header>
        <span>请求统计</span>
      </template>
      <el-table :data="requestStats" stripe>
        <el-table-column prop="name" label="请求名称" />
        <el-table-column prop="method" label="方法" width="100">
          <template #default="{ row }">
            <el-tag size="small">{{ row.method }}</el-tag>
          </template>
        </el-table-column>
        <el-table-column prop="total_requests" label="请求数" width="120" />
        <el-table-column prop="avg_response_time" label="平均响应时间(ms)" width="150" />
        <el-table-column prop="rps" label="RPS" width="100" />
        <el-table-column prop="error_rate" label="错误率" width="100">
          <template #default="{ row }">
            {{ (row.error_rate * 100).toFixed(2) }}%
          </template>
        </el-table-column>
      </el-table>
    </el-card>
  </div>
</template>

<script setup>
import { ref, reactive, computed, onMounted, onUnmounted } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import { ElMessage } from 'element-plus'
import { getRunStatus, stopRun, getRunStats } from '@/api/runs'
import LineChart from '@/components/Charts/LineChart.vue'
import PieChart from '@/components/Charts/PieChart.vue'

const route = useRoute()
const router = useRoute()
const runId = route.params.id

const scenario = ref(null)
const runStatus = ref('pending')
const stats = reactive({
  user_count: 0,
  total_requests: 0,
  requests_per_second: 0,
  error_rate: 0
})

const chartData = reactive({
  rps: [],
  errors: [],
  responseTime: []
})

const requestStats = ref([])

const statusText = computed(() => {
  const statusMap = {
    pending: '等待中',
    running: '运行中',
    completed: '已完成',
    failed: '失败',
    stopped: '已停止'
  }
  return statusMap[runStatus.value] || '未知'
})

const statusTagType = computed(() => {
  const typeMap = {
    pending: 'info',
    running: 'success',
    completed: 'primary',
    failed: 'danger',
    stopped: 'warning'
  }
  return typeMap[runStatus.value] || 'info'
})

let pollingTimer = null

const fetchStatus = async () => {
  try {
    const response = await getRunStatus(runId)
    runStatus.value = response.data.status
    Object.assign(stats, response.data.stats)
  } catch (error) {
    console.error('获取状态失败:', error)
  }
}

const fetchStats = async () => {
  try {
    const response = await getRunStats(runId)
    stats.total_requests = response.data.total_requests
    stats.requests_per_second = response.data.requests_per_second
    stats.error_rate = response.data.error_rate
    
    chartData.rps.push({
      timestamp: new Date().toLocaleTimeString(),
      value: response.data.requests_per_second
    })
    
    if (chartData.rps.length > 60) {
      chartData.rps.shift()
    }
  } catch (error) {
    console.error('获取统计失败:', error)
  }
}

const handleStop = async () => {
  await stopRun(runId)
  ElMessage.success('压测已停止')
}

const formatNumber = (num) => {
  return new Intl.NumberFormat().format(num || 0)
}

onMounted(() => {
  fetchStatus()
  pollingTimer = setInterval(() => {
    fetchStats()
    if (runStatus.value !== 'running') {
      clearInterval(pollingTimer)
    }
  }, 2000)
})

onUnmounted(() => {
  if (pollingTimer) {
    clearInterval(pollingTimer)
  }
})
</script>

<style lang="scss" scoped>
.run-monitor {
  .page-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 24px;
    
    h2 {
      margin: 0;
      font-size: 24px;
      font-weight: 600;
    }
    
    .actions {
      display: flex;
      gap: 12px;
      align-items: center;
    }
  }
  
  .stats-row {
    margin-bottom: 24px;
    
    .stat-card {
      .stat-header {
        display: flex;
        align-items: center;
        gap: 8px;
      }
      
      .stat-value {
        font-size: 32px;
        font-weight: bold;
        color: #303133;
      }
    }
  }
  
  .charts-row {
    margin-bottom: 24px;
  }
}
</style>
```

---

## 六、API 封装

### 6.1 Axios 封装

```javascript
// utils/request.js
import axios from 'axios'
import { ElMessage, ElMessageBox } from 'element-plus'
import { useUserStore } from '@/store/modules/user'
import router from '@/router'

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || '/api/v1',
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// 请求拦截器
request.interceptors.request.use(
  (config) => {
    const userStore = useUserStore()
    if (userStore.token) {
      config.headers.Authorization = `Bearer ${userStore.token}`
    }
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

// 响应拦截器
request.interceptors.response.use(
  (response) => {
    const { code, message, data } = response.data
    
    if (code === 0 || code === 200) {
      return { data }
    }
    
    ElMessage.error(message || '请求失败')
    return Promise.reject(new Error(message))
  },
  (error) => {
    const { response } = error
    
    if (response) {
      switch (response.status) {
        case 401:
          ElMessageBox.confirm('登录已过期，请重新登录', '提示', {
            type: 'warning'
          }).then(() => {
            const userStore = useUserStore()
            userStore.clearUser()
            router.push('/login')
          })
          break
        case 403:
          ElMessage.error('没有权限访问')
          break
        case 404:
          ElMessage.error('请求的资源不存在')
          break
        default:
          ElMessage.error(response.data?.message || '服务器错误')
      }
    } else {
      ElMessage.error('网络连接失败，请检查网络')
    }
    
    return Promise.reject(error)
  }
)

export default request
```

### 6.2 API 模块封装

```javascript
// api/scenarios.js
import request from '@/utils/request'

export function getScenarios(params) {
  return request.get('/scenarios/', { params })
}

export function getScenario(id) {
  return request.get(`/scenarios/${id}/`)
}

export function createScenario(data) {
  return request.post('/scenarios/', data)
}

export function updateScenario(id, data) {
  return request.put(`/scenarios/${id}/`, data)
}

export function deleteScenario(id) {
  return request.delete(`/scenarios/${id}/`)
}

export function duplicateScenario(id, name) {
  return request.post(`/scenarios/${id}/duplicate/`, { name })
}

export function importHAR(scenarioId, data) {
  const formData = new FormData()
  formData.append('file', data.file)
  if (data.resourceTypes) {
    formData.append('resource_types', data.resourceTypes)
  }
  if (data.replaceHost) {
    formData.append('replace_host', data.replaceHost)
  }
  if (data.targetHost) {
    formData.append('target_host', data.targetHost)
  }
  return request.post(`/scenarios/${scenarioId}/import-har/`, formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  })
}

export function testRequest(data) {
  return request.post('/requests/test/', data)
}
```

---

## 七、工具函数

### 7.1 日期格式化

```javascript
// utils/format.js
import dayjs from 'dayjs'
import 'dayjs/locale/zh-cn'
import relativeTime from 'dayjs/plugin/relativeTime'

dayjs.locale('zh-cn')
dayjs.extend(relativeTime)

export function formatDate(date, format = 'YYYY-MM-DD HH:mm:ss') {
  return dayjs(date).format(format)
}

export function formatRelativeTime(date) {
  return dayjs(date).fromNow()
}

export function formatDuration(seconds) {
  if (seconds < 60) {
    return `${seconds}秒`
  } else if (seconds < 3600) {
    const minutes = Math.floor(seconds / 60)
    const secs = Math.floor(seconds % 60)
    return `${minutes}分${secs}秒`
  } else {
    const hours = Math.floor(seconds / 3600)
    const minutes = Math.floor((seconds % 3600) / 60)
    return `${hours}时${minutes}分`
  }
}

export function formatNumber(num) {
  return new Intl.NumberFormat().format(num || 0)
}
```

### 7.2 表单验证

```javascript
// utils/validate.js
export const validators = {
  required: (message = '该项为必填') => ({
    required: true,
    message,
    trigger: 'blur'
  }),
  
  email: () => ({
    type: 'email',
    message: '请输入正确的邮箱地址',
    trigger: 'blur'
  }),
  
  url: () => ({
    type: 'url',
    message: '请输入正确的 URL 地址',
    trigger: 'blur'
  }),
  
  minLength: (min, message) => ({
    min,
    message: `长度不能少于 ${min} 个字符`,
    trigger: 'blur'
  }),
  
  maxLength: (max, message) => ({
    max,
    message: `长度不能超过 ${max} 个字符`,
    trigger: 'blur'
  }),
  
  pattern: (regex, message) => ({
    pattern: regex,
    message,
    trigger: 'blur'
  })
}

export const patterns = {
  username: /^[a-zA-Z][a-zA-Z0-9_]{3,19}$/,
  password: /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)[^]{8,32}$/,
  phone: /^1[3-9]\d{9}$/,
  ip: /^(\d{1,3}\.){3}\d{1,3}$/
}
```

---

## 八、样式规范

### 8.1 全局样式

```scss
// assets/styles/variables.scss
$primary-color: #409EFF;
$success-color: #67C23A;
$warning-color: #E6A23C;
$danger-color: #F56C6C;
$info-color: #909399;

$text-primary: #303133;
$text-regular: #606266;
$text-secondary: #909399;
$text-placeholder: #C0C4CC;

$border-color: #DCDFE6;
$border-color-light: #E4E7ED;
$border-color-lighter: #EBEEF5;

$background-color: #F5F7FA;

// assets/styles/index.scss
@import './variables.scss';

* {
  box-sizing: border-box;
}

html, body {
  margin: 0;
  padding: 0;
  height: 100%;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial,
    'Noto Sans', sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol',
    'Noto Color Emoji';
  font-size: 14px;
  color: $text-primary;
  background-color: $background-color;
}

a {
  color: $primary-color;
  text-decoration: none;
  
  &:hover {
    color: lighten($primary-color, 10%);
  }
}

// 页面容器
.page-container {
  padding: 24px;
}

.page-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24px;
  
  h2 {
    margin: 0;
    font-size: 24px;
    font-weight: 600;
  }
}

// 卡片样式
.card {
  background: #fff;
  border-radius: 4px;
  padding: 20px;
}

// 过渡动画
.fade-enter-active,
.fade-leave-active {
  transition: opacity 0.2s ease;
}

.fade-enter-from,
.fade-leave-to {
  opacity: 0;
}
```
